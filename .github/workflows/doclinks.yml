# This file was generated from bswck/skeleton@c9a43b3.
# Instead of changing this particular file, you might want to alter the template:
# https://github.com/bswck/skeleton/tree/c9a43b3/project/.github/workflows/%23%25%20if%20docs%20%25%23doclinks.yml%23%25%20endif%20%25%23.jinja

name: "readthedocs/actions"
on:
  pull_request_target:
    types:
    - "opened"
    paths:
    - "docs/**"
    - "CHANGELOG.md"

permissions:
  pull-requests: "write"

jobs:
  documentation-links:
    runs-on: "ubuntu-latest"
    steps:
    - uses: "readthedocs/actions/preview@v1"
      with:
        project-slug: "lazy-imports"